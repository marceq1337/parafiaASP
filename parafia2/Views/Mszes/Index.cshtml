@model IEnumerable<parafia2.Models.DataLayer.Msze>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataMszy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KsiadzNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MinistrantNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataMszy)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KsiadzNavigation.Imie)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.MinistrantNavigation.Imie)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
